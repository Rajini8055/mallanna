
    //
    // //  const win = winners.map(function(name){
    // //     return (name + 'bos');
    // //  });
    //
    var winners = ['kumar','swamy','gurijala'];
    // /* Arrow function*/
    var win = winners.map((name)=>`${name} bos`;
    console.log(win);



    // var race ='100m race';

    // var win = winners.map((name,i)=>({name:name,race:race,place:i+1}));
    //  console.log(win);


/*Filters*/

// const ages = [25,35,45,85,75,95,65,48,52,35,62,51,42,53];
//  const old = ages.filter(age => age >=50);
// console.log(old);

/*
const names = ['wes', 'kait', 'lux'];
const fullNames = names.map(function(name) {
  return `${name} bos`;
});
console.log(fullNames);
  */
